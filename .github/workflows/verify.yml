# =============================================================================
# .github/workflows/verify.yml — the fast tier, every push and PR. A script or
# doc change must not wait on a container build: this is the contract suite
# plus a BuildKit lint that resolves every stage without building one.
# Anything needing a real image lives in images.yml.
# =============================================================================
name: verify

# The token can read the checkout and nothing else.
permissions:
  contents: read

on:
  push:
    branches: ['**']
  pull_request:
  workflow_dispatch:

# A newer push supersedes an in-flight run of the same ref.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  contracts:
    name: contracts + Dockerfile lint
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5
      # The contract suite. Needs neither docker nor .env.
      - run: make verify
      # A fork that did what GETTING_STARTED says — adopted its name, removed
      # the starters, replaced README — must still pass: the template's own
      # content is checked only upstream (upstream_checks in the suite).
      # The runners ship python 3.12/3.13, but 22.04 — the kit's own default base
      # — ships 3.10, where tomllib does not exist. Every TOML-parsing check
      # printed nothing there and the contracts read that silence as a finding.
      # Hide the module and require the suite to SKIP those checks, not fail.
      - name: the suite degrades where tomllib is absent (python < 3.11)
        run: |
          mkdir -p "$RUNNER_TEMP/no-tomllib"
          printf 'raise ImportError("hidden for this run")\n' > "$RUNNER_TEMP/no-tomllib/tomllib.py"
          out="$(PYTHONPATH="$RUNNER_TEMP/no-tomllib" make verify 2>&1)" || {
            echo "$out" | tail -20; echo "::error::make verify fails without tomllib"; exit 1; }
          grep -q 'No tomllib' <<< "$out" \
            || { echo "::error::the suite ran the TOML checks anyway, or did not say it skipped them"; exit 1; }

      - name: the suite passes on a fork that made every documented edit
        run: |
          cp -a . /tmp/fork && cd /tmp/fork
          # A fresh fork has no .env yet; carrying a developer's copy in
          # would make adopt guard the volumes of the ORIGINAL project.
          rm -f .env
          make adopt NAME=forkprobe DESC='fork probe'
          # Everything GETTING_STARTED and DEPENDENCIES tell a fork it may do
          # WITHOUT a container: own the starters, README and docs/, uncomment
          # the opt-in packages, set site SLURM values, pin the base image.
          # (Uncommenting the torch extras needs a relock, which needs mksync;
          # [adopt] checks that example parses on its own.)
          rm -rf src/example docs && printf '# forkprobe\n' > README.md
          sed -i 's/^# terminator # gui/terminator # gui/' dependencies/apt.txt
          printf 'DEVKIT_SLURM_CPUS_PER_TASK=16\n' >> .env.example
          printf 'BASE_IMAGE=ubuntu:22.04@sha256:%064d\n' 0 >> .env.example
          make verify
      # The docker-build shell has no terminal and no BASH_ENV, and the prod
      # builder stages source config/util_aliases.sh in exactly that shell —
      # a host run passes even when that path is broken.
      - name: container shell bootstrap (no tty, no BASH_ENV)
        run: |
          docker run --rm -v "$PWD:/w:ro" -w /w -e WORKSPACE_PATH=/w ubuntu:22.04 bash -c '
            set -e
            source /w/config/util_aliases.sh
            for fn in mksync cbuild mbuild uvs mkenv log_ok log_detail; do
              declare -F "$fn" >/dev/null || { echo "missing function: $fn"; exit 1; }
            done
            [ -n "$WS_VENV_PY" ] || { echo "WS_VENV_PY unset"; exit 1; }
            log_warn probe 2>&1 | cat -v | grep -q "\^\[" && { echo "ANSI escape in a build log"; exit 1; }
            mkdir -p /tmp/w/install/x && : > /tmp/w/install/x/f
            bash /w/scripts/check_deps.sh /tmp/w/install'
      # ubuntu-latest IS a native Linux host, so the detector's non-WSL branch
      # needs no simulation here — only on a WSL2 developer machine, where
      # /proc/version cannot be replaced, is this unreachable.
      - name: host detection resolves on a native Linux host
        run: |
          bash scripts/check_env.sh --makefile > env.mk
          grep -q '^IS_WSL := false'   env.mk || { echo "IS_WSL should be false here"; cat env.mk; exit 1; }
          grep -q '^IS_MACOS := false' env.mk || { echo "IS_MACOS should be false here"; exit 1; }
          # No GPU on a hosted runner: the three flags must all say so, and the
          # Makefile must then resolve the profile to cpu rather than guessing.
          for flag in 'HAS_NVIDIA := false' 'HAS_TOOLKIT := false' 'HAS_DRI := false'; do
            grep -qF "$flag" env.mk || { echo "expected '$flag' on this runner"; cat env.mk; exit 1; }
          done
          make -n start ENV=ros >/dev/null
          # The WSL-only mounts must be inert, or compose would bind a device
          # that exists on no native host.
          grep -q '^HOST_DXG_MOUNT := /dev/null:/dev/null' env.mk \
            || { echo "the WSL DirectX mount is not neutralised on native Linux"; grep DXG env.mk; exit 1; }

      # --check lints and resolves every stage; nothing is built.
      - name: docker build --check
        run: |
          docker build --check -f docker/Dockerfile \
            --build-arg BASE_IMAGE=ubuntu:22.04 \
            --build-arg ROS_DISTRO=humble .

  # The host half of DevKit runs on the developer's machine, where sed, awk,
  # stat and date are BSD, not GNU. The suite is offline and needs no docker,
  # so the whole host contract is checkable on a real macOS runner for ~1 min.
  macos-host:
    name: macOS host (BSD sed/awk/stat)
    runs-on: macos-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
      - run: make verify
      # Detection must survive a host with no /sys, no /proc and no docker.
      - name: host detection resolves on macOS
        run: |
          bash scripts/check_env.sh --makefile > env.mk
          grep -q '^IS_MACOS := true' env.mk || { echo "IS_MACOS not detected"; cat env.mk; exit 1; }
          grep -q '^HOST_UID := ' env.mk || { echo "HOST_UID missing"; exit 1; }
          # GPU_MODE is an INPUT, not an emitted key: the Makefile resolves the
          # profile from the three flags below. macOS must land on all-false.
          for flag in 'HAS_NVIDIA := false' 'HAS_TOOLKIT := false' 'HAS_DRI := false'; do
            grep -qF "$flag" env.mk || { echo "expected '$flag' on macOS"; cat env.mk; exit 1; }
          done
          make -n start ENV=ros >/dev/null
      - name: every CLI still answers --help under BSD userland
        run: |
          for s in scripts/*.sh; do
            bash "$s" --help >/dev/null 2>&1 || { echo "$s --help failed on macOS"; exit 1; }
          done
